# No external database is required.
# Links are stored in-memory (persist for the lifetime of each serverless function instance).
# Copy this file to .env.local if you need to add any local environment overrides.
