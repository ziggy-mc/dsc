# dsc
discord url shortner
ps copilot helped me start, from now on I'll update the code

## Environment

Copy `.env.local.example` to `.env.local` and fill in all variables.

`BLOB_READ_WRITE_TOKEN` is required if you want uploaded report images to be stored and shown in the admin panel.
